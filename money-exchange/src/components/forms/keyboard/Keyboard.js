import React from "react";

class Keyboard extends React.Component {
    render() {
        return (
            <div>
                module: Keyboard
            </div>
        );
    }
}

export default Keyboard;
